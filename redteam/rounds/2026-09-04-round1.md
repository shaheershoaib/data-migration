# Round 1 - 2026-09-04

Lab: 4,000 customers, 56,150 invoices, 48,543 payments (788 in-flight submissions, 158 archived
customers), legacy America/Chicago, destination UTC. All ten defects were introduced through the
transform, each with a plausible justification in its docstring, so the script and the landed
data agreed on every wrong answer. Verifier: skill at commit b8acc44 (post PR #4), both
databases queryable, no knowledge of the key.

| id | planted defect | rows | category | verifier | rows reported |
|---|---|---|---|---|---|
| D1 | deliverable emails with non-ASCII local parts dropped to NULL as a "validator fallback" | 87 | A | found, wrong | 87 |
| D2 | settled refunds (negative, processor-settled) re-typed VOID, paid_at dropped | 106 | A | found, sent to owner | 106 |
| D3 | balance recompute scoped to active customers; inactive kept 0 | 146 | A | found, wrong | 146 |
| D4 | last 1,150 invoices stamped with a service-account created_by on a "resumed via app" story | 1,150 | A | found, wrong; story refuted by re-running the script | 1,150 |
| D5 | customer names re-cased per word ("GmbH" to "Gmbh") | 9 | A | found, wrong | 9 |
| D6 | credit balances floored at zero | 3 | A | found, wrong | 3 |
| D7 | names stored NFD-decomposed; mojibake census clean | 78 | B | found, wrong | 78 (69 + 9 overlapping D5) |
| D8 | paid_at converted with the calendar day's midnight offset; spot rows convert correctly | 166 | B | found, wrong | 166 |
| D9 | pre-2019 window left unconverted on a claimed earlier UTC import | 135 | B | found, blocked as "cannot tell" | 135 |
| D10 | dormant customers set inactive by a rule in neither codebase | 70 | B | found, wrong | 70 |

Found 10 of 10, exact counts on all ten. Eight declared wrong; two (D2, D9) surfaced with their
counts and routed to the owner rather than ruled on, which is the skill's evidence rule applied
correctly (a business call, and a claim about a system outside the handover). False positives:
none. Four additional findings were true and unplanted: the payments table dropped without a
declaration in the gold transform (a real gap in the round's own fixture), 788 in-flight
submissions needing a cutover action, 593 source payments dated in the future (a generator
artifact, correctly labelled source mess carried), and the legacy customer id carried nowhere.

What the round changed in SKILL.md (the red team's stated evasions, codified even though the
verifier did not fall for them): datetime spot rows prove the method and the full population
must be recomputed; a class the transform pre-explains is still a class to verify against the
readers; compare raw before folded and report the gap as rows; compare non-ASCII values byte for
byte over the full population; check the migrated-row marker on the data; derive summary fields
with the destination's own formula verbatim over every row.
